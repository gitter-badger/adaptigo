{
	"icon": "numix",
	"label": "Numix themes",
	"description": "GTK and icon themes from the Numix Project.",
	"license": "GPLv3",
	"category": "Themes",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "dpkg-query -l numix-icon-theme numix-icon-theme-circle numix-gtk-theme " }
	}
}
