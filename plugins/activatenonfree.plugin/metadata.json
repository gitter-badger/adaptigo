{
	"icon": "system",
	"label": "Activate non-free contrib repo",
	"description": "add the non-free and contrib repo",
	"category": "Tweaks",
	"scripts": {
		"exec": {
			"label": "Setup",
			"command": "run-as-root -s setup.sh"
		},
		"undo": {
			"label": "Revert",
			"command": "run-as-root rm -f /etc/profile.d/color_prompt.sh"
		},
		"status": { "command": "test -f /etc/profile.d/color_prompt.sh" }
	}
}
